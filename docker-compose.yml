services:
  myresumo:
    image: ghcr.io/analyticace/myresumo:latest
    container_name: myresumo
    restart: unless-stopped
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080"]
      interval: 30s
      timeout: 10s
      retries: 3